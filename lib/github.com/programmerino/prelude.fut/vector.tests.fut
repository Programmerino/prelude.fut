-- | ignore
import "vector"

-- ==
-- entry: map amap tabulate foldr foldl or and replicate indices aindices iota rotate arotate length concat_to concat concat_array_to concat_array test
-- structure seq-mem { Copy 0 Concat 0 Alloc 0 Scratch 0 }